//create two arrays and merge them into a single array
let arr1 = [3,4,5,6];
let arr2 = [9,10,22,23];

let arr3 = arr1.concat(arr2);   //it will create a new array arr3, which will be a merger of arr1 and arr2
console.log(arr3);